# CityPulse AI — gameful Streamlit MVP
streamlit>=1.30
scikit-learn>=1.3
lightgbm>=4.0
pandas>=2.0
numpy>=1.24
joblib>=1.3
# Optional: only used if OPENAI_API_KEY is set (the app runs fully without it)
openai>=1.0
