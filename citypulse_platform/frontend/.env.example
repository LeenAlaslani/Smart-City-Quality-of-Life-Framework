# Frontend → backend API base.
# Local dev leaves this unset (Vite proxies /api to the backend on :8600).
# Production (separate backend origin) MUST set it to the backend URL + /api:
# VITE_API_BASE=https://citypulse-backend.onrender.com/api
